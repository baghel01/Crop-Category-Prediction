# -*- coding: utf-8 -*-
"""Untitled7.ipynb

Automatically generated by Colaboratory.

Original file is located at
    https://colab.research.google.com/drive/1lE_ZQJ1bEOKHVs4WmQaMWx0ZTgXOhWSr
"""

import pandas as pd
import numpy as np
import os
!sudo apt-get install unzip
!unzip /content/cdlvalue.zip -d /home
a=pd.read_csv("/home/cdlvalue.csv")
!unzip /target.zip -d /home
def array2(path=None):
    files=os.listdir(path)
    files.sort()
    temp=[]
    for i in files:
        img=plt.imread(path+i)
        img=img.flatten()
        temp.append(img)
        
    return np.array(temp)
Y=array2("/home/target/")
#Y.shape
np.unique(Y)
from collections import Counter
freq = Counter(np.array(Y).flatten())
print(freq)
a
import matplotlib.pyplot as plt
import seaborn as sns
#plt.plot(x,y)
#bar=plt.bar(x,y,width=9)
l=len(freq)
plt.bar(range(l), freq.values(), align='edge',width=0.6)
plt.xlabel('cropname')
plt.ylabel('pixel value')
x=[]
y=[]
for i in freq:
  x.append(i)
  y.append(freq[i])
x=np.array(x)
y
np.array(x).shape
for i, v in enumerate(labels):
    axes.text(i-.25, 
              v/labels[i]+100, 
              labels[i], 
              fontsize=18, 
              color=label_color_list[i])